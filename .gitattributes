# Enforce LF regardless of a contributor's global core.autocrlf, matching .editorconfig's
# end_of_line = lf. Without this, a checkout with core.autocrlf=true silently converts everything
# to CRLF, which then fails `dotnet format --verify-no-changes` in CI.
* text=auto eol=lf
